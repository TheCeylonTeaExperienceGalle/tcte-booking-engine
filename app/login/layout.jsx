export const metadata = {
  title: "Staff sign in | TCTE Booking",
  description: "Sign in to the TCTE booking administration dashboard.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
